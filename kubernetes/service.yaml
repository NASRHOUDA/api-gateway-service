apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: microservices
spec:
  selector:
    app: api-gateway
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
